On the main working directory, execute:

### bundle
`./crates/key-vault-gui/scripts/build-and-sign.sh --profile <your provision profile file>`

### bundle release
`./crates/key-vault-gui/scripts/build-and-sign.sh --release --profile <your provision profile file>`

### run the bundle
`open target/debug/Key\ Vault.app`
